@using XX_PhotoPrint.Service
@using System.Web.Script.Serialization
@{
    Layout = null;

    var js = new JavaScriptSerializer();

    Output.Write(js.Serialize(new { success = true, data = PaymentService.Payments }));
    return;
}
